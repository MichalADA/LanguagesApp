# --- Application ---
NODE_ENV=development
PORT=3000

# --- Database ---
# Local host use: postgresql://lexo:lexo@localhost:5432/lexodromia?schema=public
# Docker compose:  postgresql://lexo:lexo@postgres:5432/lexodromia?schema=public
DATABASE_URL=postgresql://lexo:lexo@localhost:5432/lexodromia?schema=public

# --- Auth ---
# Replace these with long random strings in production. Never commit real secrets.
JWT_SECRET=development-access-secret-change-before-production
JWT_ACCESS_EXPIRATION=15m
JWT_REFRESH_SECRET=development-refresh-secret-change-before-production
JWT_REFRESH_EXPIRATION=30d

BCRYPT_SALT_ROUNDS=12

# Refresh token is never exposed to JavaScript. Set secure=true behind HTTPS.
AUTH_COOKIE_NAME=lexodromia_refresh
AUTH_COOKIE_SECURE=false
AUTH_REFRESH_COOKIE_MAX_AGE_MS=2592000000

# --- CORS ---
# Comma-separated list of allowed origins for the frontend.
CORS_ORIGIN=http://localhost:5173,http://localhost:8080
